# ===============================
# 기본 앱 설정
# ===============================
spring.application.name=mentoring-backend
server.port=8080

# ===============================
# DataSource (MySQL 연결 설정)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/mentoring_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
spring.datasource.username=mentoring_user
spring.datasource.password=As2025!!

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate 설정
# ===============================
# 지금은 엔티티 기준으로 테이블을 새로 생성 (DB 초기화용)
spring.jpa.hibernate.ddl-auto=validate

# MySQL 방언 (적어도 되지만, 요즘은 자동 감지라 없어도 됨)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 콘솔에 SQL 로그 찍기 (개발용)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 시간대
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul

# ===============================
# Flyway (지금은 일단 비활성화)
# ===============================
spring.flyway.enabled=false

# ===============================
# JWT 설정
# ===============================
jwt.secret=QRU9+/dWzTNqhXSImrTqOTgX+/Bda6qkoqNCDSkWGI3rOS6IdQpoHss9t/mUwa/tD5xprD/WhQ/u8b+pMs+kOw==
jwt.expiration-ms=3600000

# ===============================
# (선택) Security 로그 레벨 (디버깅용)
# ===============================
# logging.level.org.springframework.security=DEBUG
